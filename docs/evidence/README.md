# 已脱敏的验证摘要

本目录只保留可复现的结论，不保留原始真机日志。原始日志中的设备型号、设备标识、进程号、内存地址、连接目标、签名细节和系统服务日志均已移除，避免随源码公开。

## JIT 探针

- HarmonyOS/OpenHarmony arm64 测试设备：RW→RX、生成代码执行、重复调用、跨线程执行和释放后重建均通过。
- FORT/MAP_JIT 对照项按预期不作为公开接口使用。
- 设备安装、签名和权限细节请在本地重新采集；不要把原始 `hdc` 输出提交到仓库。

## 发布包边界

- `.hap/.hsp`、证书、私钥、profile、BIOS、游戏镜像、存档和原始日志均不入库。
- 如需提交新的验证记录，只保留脱敏后的版本、平台、架构、结果和错误类别。
