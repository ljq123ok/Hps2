# HPS2 阶段 1 —— 真机 JIT 策略取证记录
# 设备: HUAWEI Pura X View (VOL-AL00) / OpenHarmony-7.0.0.105 / API 26
# 采集时间: 2026-09-14T05:59:07Z

## 1. 内核对照：真机 vs 模拟器（关键差异）
真机 : [Fail]Not match target founded, check connect-key please  [掉线前采集]
      内核 = HongMeng Kernel 1.13.0
模拟器: Linux localhost 5.10.210 #1 SMP Tue Jul 21 15:26:51 CST 2026 aarch64 Toybox
      内核 = Linux 5.10.210（标准 Linux）

## 2. JITFort 内核节点对照
真机   : /proc/sys/kernel/jitfort/jitfort_mode  -> 存在（dr-xr-xr-x root root）
         读取结果: Permission denied（root-only）
模拟器 : /proc/sys/kernel/jitfort -> No such file or directory

## 3. 真机/xpm 节点
真机   : /proc/sys/kernel/xpm/xpm_mode -> 存在（root-only）
模拟器 : 无 xpm 节点

## 4. 真机 Shell 域限制
- /data/local/tmp 下静态 aarch64 二进制执行被拒: Permission denied
  （SELinux 上下文为 u:object_r:data_local_tmp:s0）
- /data 挂载: nosuid,nodev（无 noexec，但 SELinux 域限制执行）
- dmesg: klogctl: Operation not permitted
- /proc/<其它进程>/maps: Operation not permitted（/proc/self/maps 可读）
- /sys/fs/selinux/policy: Permission denied

## 5. 待重做的事项（本轮被设备掉线打断）
- 全量扫描设备上 244 个应用是否持有 JIT 类权限
  ⚠️ 本轮扫描结果 **无效**：循环期间设备掉线，bm dump 仅返回 46 字节错误，
     被误判为「无该类权限」。需在设备稳定后重做。
  （已用 aobai 应用验证 bm dump 确实会输出 ohos.permission 条目，
    说明方法本身可行，问题在设备连接稳定性）
