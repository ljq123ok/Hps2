# 构建产物
**/build/
**/.cxx/
**/oh_modules/
**/node_modules/
**/.hvigor/
**/.preview/
*.hap
*.har
*.hsp
*.apk
*.ipa
*.dmp
*.pcap
*.trace
*.log

# 上游源码（按提交号可重现；不纳入本仓库历史）
upstream/

# 宿主测试二进制
stage1-jitprobe/host-test/encode_check

# macOS
.DS_Store
.idea/

# 签名材料 —— 绝不入库
*.p12
*.p7b
*.cer
*.csr
.signing/

# release 签名私钥与证书 —— 绝不入库
.release-signing/

# 真机数据备份（含用户存档：memcards/sstates）—— 绝不入库。
# 这是阶段 0 探针前的预防性备份，属个人数据，不入仓库。
.backup-probe-precaution/

# 第三方交叉编译产物（可从源码重建）
thirdparty-ohos/src/
thirdparty-ohos/build/
thirdparty-ohos/prefix/
thirdparty-ohos/logs/

# ============================================================================
# 版权内容 —— 绝不入库、绝不随发行版分发
#
# BIOS 是索尼的版权作品；游戏镜像同理。本应用**不内置、不分发**它们，
# 用户需自行准备合法拥有的副本（见 README 的法律声明）。
#
# 这些规则同时是**防御性**的：防止误把本地测试用的 BIOS/ROM 提交进去。
# ============================================================================
PS2 BIOS 全文件/
*.iso
*.ISO
*.chd
*.CHD
*.bin
*.BIN
*.rom
*.ROM
*.nvm
*.NVM
*.mec
*.MEC
*.p2b

# 例外：源码与资源（上面的 *.bin/*.rom 可能误伤，这里放行）
!app-hps2/**/*.bin
!app-hps2/**/*.rom
!thirdparty-ohos/**/*.bin
